# Black Vault - Environment Variables Template
# Copy this file to .env and fill in the values

# --- API Keys ---
# Required for embeddings (gemini-embedding-001)
GEMINI_API_KEY=your_gemini_api_key_here

# --- LLM Configuration (Ollama) ---
# Default host for the self-hosted Ollama instance
OLLAMA_HOST=10.8.0.3:11434
# Model name to use in Ollama
LLM_MODEL=llama3.2

# --- Database ---
# Path to the DuckDB file (defaults to black_vault.duckdb in src/)
# BLACK_VAULT_DB=black_vault.duckdb
